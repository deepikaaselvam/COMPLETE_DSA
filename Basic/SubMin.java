package Basic;

public class SubMin {
    int ToFindMininumValue=Integer.MAX_VALUE;
    int sum=0;
    public static void main(String[] args) {
        int[]array={3,1,2,4};
        for(int i=0;i<array.length;i++)
        {
            for(int j=i;j<array.length;j++)
            {
                // sum=Math.min(ToFindMininumValue,array[i]);

            }
        }
    }
    
}
